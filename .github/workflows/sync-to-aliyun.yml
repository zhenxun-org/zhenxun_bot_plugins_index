name: Force Sync to Aliyun
on:
  push:
    branches: ["index"]
jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global http.postBuffer 524288000  
          git config --global core.compression 0

      - name: Add aliyun remote
        run: |
          git remote add aliyun https://${{secrets.ALIYUN_ACCOUNT}}:${{secrets.ALIYUN_PASSWORD}}@codeup.aliyun.com/69624f9a58acb3fac7aed520/zhenxun_plugins.git
          git fetch aliyun test2me --force  # 强制更新本地引用

      - name: Force push
        run: git push --progress --force aliyun HEAD:test2me
